{% extends 'base.html.twig' %}

{% block title %}Détail du participant{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">👤 Détail du Participant</h1>

        <div class="card">
            <div class="card-header bg-primary text-white">
                <h2 class="h5 mb-0">{{ participant.fullName }}</h2>
            </div>
            <div class="card-body">
                <p><strong>Email:</strong> {{ participant.email }}</p>
                <p><strong>Téléphone:</strong> {{ participant.phoneNumber ?: '—' }}</p>
                <p><strong>Campus:</strong> {{ participant.campus ?: '—' }}</p>
                <p><strong>Rôle:</strong>
                    {% if participant.role == 'admin' %}
                        <span class="badge bg-danger">Admin</span>
                    {% elseif participant.role == 'guest' %}
                        <span class="badge bg-secondary">Invité</span>
                    {% else %}
                        <span class="badge bg-primary">{{ participant.role }}</span>
                    {% endif %}
                </p>
                <p><strong>Activité:</strong> {{ participant.activity ?: '—' }}</p>
                <p><strong>Inscription:</strong> {{ participant.registrationDate ? participant.registrationDate|date('d/m/Y') : '—' }}</p>
            </div>
            <div class="card-footer text-end">
                <a href="{{ path('participant_list') }}" class="btn btn-secondary">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
