{% extends 'base.html.twig' %}

{% block title %}Détail de l'utilisateur{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center mb-4">👤 Détail de l'utilisateur</h1>

        <div class="row justify-content-center">
            <div class="col-md-6 col-lg-5">
                <div class="card shadow-sm">
                    <div class="card-header bg-primary text-white text-center">
                        <h2 class="h5 mb-0">{{ user.firstName }} {{ user.lastName }}</h2>
                    </div>
                    <div class="card-body">
                        <dl class="row mb-0">
                            <dt class="col-5 text-end">Email :</dt>
                            <dd class="col-7 text-start">{{ user.email }}</dd>

                            <dt class="col-5 text-end">Pseudo :</dt>
                            <dd class="col-7 text-start">{{ user.pseudo }}</dd>

                            <dt class="col-5 text-end">Téléphone :</dt>
                            <dd class="col-7 text-start">{{ user.phone ?: '—' }}</dd>

                            <dt class="col-5 text-end">Campus :</dt>
                            <dd class="col-7 text-start">{{ user.campus ? user.campus.campusName : '—' }}</dd>

                            <dt class="col-5 text-end">Rôle :</dt>
                            <dd class="col-7 text-start">
                                {% if 'ROLE_ADMIN' in user.roles %}
                                    <span class="badge bg-danger">Admin</span>
                                {% else %}
                                    <span class="badge bg-secondary">Invité</span>
                                {% endif %}
                            </dd>
                        </dl>
                    </div>
                    <div class="card-footer d-flex justify-content-center gap-2">
                        {% set canEditDelete = user.isActive and 'ROLE_ADMIN' not in user.roles %}

                        {% if canEditDelete %}
                            <a href="{{ path('admin_edit_user', {id: user.id}) }}" class="btn btn-warning btn-sm">Modifier</a>
                            <a href="{{ path('admin_user_delete', {id: user.id}) }}" class="btn btn-danger btn-sm"
                               onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet utilisateur ?');">Supprimer</a>
                        {% else %}
                            <button class="btn btn-warning btn-sm" disabled>Modifier</button>
                            <button class="btn btn-danger btn-sm" disabled>Supprimer</button>
                        {% endif %}
                        <a href="{{ path('admin_users_list') }}" class="btn btn-secondary">
                            Retour à la liste
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
