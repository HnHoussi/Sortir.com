{% extends 'base.html.twig' %}

{% block title %}Détail de l'User{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">👤 Détail de l'User</h1>

        <div class="card">
            <div class="card-header bg-primary text-white">
                <h2 class="h5 mb-0">{{ user.fullName }}</h2>
            </div>
            <div class="card-body">
                <p><strong>Email:</strong> {{ user.email }}</p>
                <p><strong>Téléphone:</strong> {{ user.phone ?: '—' }}</p>
                <p><strong>Campus:</strong> {{ user.campus ?: '—' }}</p>
                <p><strong>Rôle:</strong>
                    {% if user.role == 'admin' %}
                        <span class="badge bg-danger">Admin</span>
                    {% elseif user.role == 'guest' %}
                        <span class="badge bg-secondary">Invité</span>
                    {% else %}
                        <span class="badge bg-primary">{{ user.role }}</span>
                    {% endif %}
                </p>
                <p><strong>Activité:</strong> {{ user.activity ?: '—' }}</p>
                <p><strong>Inscription:</strong> {{ user.registrationDate ? user.registrationDate|date('d/m/Y') : '—' }}</p>
            </div>
            <div class="card-footer text-end">
                <a href="{{ path('user_list') }}" class="btn btn-secondary">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
