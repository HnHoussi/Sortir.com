<h1>Salut !</h1>

<p>Pour réinitialiser ton mot de passe, clique sur ce lien :</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p>Attention, ce lien n’est valide que pendant {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }} — alors fais vite pour ne manquer aucun événement !</p>

<p>À bientôt et ne rate pas les bonnes sorties !</p>
